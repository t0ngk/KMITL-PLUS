# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

นักศึกษา สจล. (KMITL) ที่ล็อกอินระบบทะเบียน `reg.kmitl.ac.th` อยู่แล้ว รวมถึงศิษย์เก่าที่เมนูของ reg หายไปแล้วแต่ยังเข้าถึงข้อมูลย้อนหลังได้

งานหลักที่ผู้ใช้เปิดขึ้นมาทำ (ยืนยันกับเจ้าของโปรเจกต์):

1. **ดูตารางตัวเองเร็ว ๆ** — เปิดมาเช็กว่าวันนี้/สัปดาห์นี้เรียนอะไร ห้องไหน เวลาไหน แล้วปิด
2. **เซฟรูปไปแชร์** — แต่งสีรายวิชา แล้วโหลดเป็น PNG ไปโพสต์/ส่งต่อ

ทั้งสองงานมีน้ำหนักเท่ากัน หน้าเดียวกันต้องรับใช้ทั้ง "อ่านเร็วบนจอ" และ "เป็นรูปที่ดูดีนอกจอ"

## Product Purpose

Chrome Extension (MV3) ที่แทนที่หน้าตารางของระบบทะเบียน KMITL ด้วยหน้าที่ออกแบบใหม่ ทำงานเป็น content script บนหน้าเว็บของ reg โดยตรง ไม่มี backend ไม่มีบัญชีผู้ใช้ ไม่มีการเก็บข้อมูลออกนอกเครื่อง

สำเร็จ = นักศึกษาเปิดหน้า reg แล้วได้ตารางที่อ่านออกทันทีโดยไม่ต้องคิด และได้รูปที่กล้าโพสต์

โปรเจกต์เป็น open source รับ contribution ทั้งด้าน code และ UX/UI (ติดต่อ IG `t0ng.k`)

## Positioning

ไม่ใช่แอปตารางเรียนที่ต้องกรอกข้อมูลเอง และไม่ใช่เว็บแยกที่ต้องล็อกอินใหม่ — มันคือหน้าเดิมของ reg ที่ถูกแทนที่ในที่เดิม ข้อมูลจริงของนักศึกษาคนนั้น ไม่ต้องพิมพ์อะไรเลย ใช้ session ที่ล็อกอินอยู่แล้ว

## Operating Context

- ทำงานเฉพาะเมื่อผู้ใช้เปิด URL เหล่านี้บนเดสก์ท็อป Chrome:
  - `https://*.reg.kmitl.ac.th/u_student/report_studytable_show.php` (ตารางเรียน)
  - `https://*.reg.kmitl.ac.th/u_student/report_examtable_show.php` (ตารางสอบ)
- ข้อมูลทั้งหมดมาจากการ scrape HTML ของ reg และการ fetch เพิ่มด้วย session เดิม (`credentials: same-origin`)
- หน้าเว็บของ reg เป็นระบบเก่า: ส่งข้อมูลเป็น **windows-874 (TIS-620)** ต้อง decode เอง ไม่งั้นภาษาไทยเพี้ยน
- โครงสร้าง HTML ของ reg เปลี่ยนได้โดยไม่บอกล่วงหน้า — ทุกฟีเจอร์ต้องพังแบบปลอดภัย (แสดงหน้าเดิม ไม่ใช่จอขาว)
- ปลายทางของรูปที่เซฟ: แชร์ในโซเชียล/แชท จอมือถือเป็นหลัก

## Capabilities and Constraints

ความสามารถที่มีอยู่และต้องคงไว้ทั้งหมด:

- **ตารางเรียน** — กริดรายสัปดาห์ จ.–อา. ช่องเวลา 15 นาที 08:00–20:00 บล็อกวิชากินช่วงคาบจริง แสดงชื่อวิชา section ประเภท (ท/ป) ห้อง/อาคาร และเวลาเริ่ม–จบ
- **หัวตาราง** — คณะ, ภาควิชา + สาขา, ภาคเรียน + ปีการศึกษา, รหัส + ชื่อนักศึกษา (scrape มาจากหน้า)
- **ตารางสอบ** — รายการจัดกลุ่มตามวันสอบ เรียงตามเวลาเริ่ม แสดงวันที่ (พ.ศ.), เวลา, รหัสวิชา, ชื่อวิชา, section, หน่วยกิต, ประเภทสอบ, ห้อง; สลับ Mid Term / Final ได้
- **สลับปี/ภาคเรียน** ในหน้าตารางเรียน โดยไม่ reload — ตัวเลือกปีต้องมาจาก server ของ reg เท่านั้น ห้าม hardcode
- **แต่งสี** — วิชาละสีจาก palette คงที่ เปลี่ยนสีรายวิชาและสีหัวตารางได้ทีละอัน + reset
- **Export PNG** — snapdom capture เฉพาะ header + ตาราง; ปุ่มลอย/picker/select ต้องไม่ติดไปในรูป (select ในพื้นที่ capture ถูกแทนด้วย text ธรรมดา)
- **ปุ่ม Old Design** — กลับไปดู HTML เดิมของหน้าที่เก็บไว้ตอน boot แล้วสลับกลับมาได้

ข้อจำกัดทางเทคนิคที่ผูกกับดีไซน์:

- MV3 **ห้ามโหลด asset จาก CDN ภายนอก** — ฟอนต์ Prompt bundle มากับ extension และถูก inject เป็น `<style>` inline ใน DOM ของหน้า (ไม่งั้น snapdom มองไม่เห็น `@font-face` ตอน export)
- ทุกอย่างที่ต้องขึ้นในรูป PNG ต้องเป็นสิ่งที่ snapdom render ได้ — เอฟเฟกต์ที่ capture ไม่ติดใช้ได้เฉพาะนอกพื้นที่ capture
- ต้องรองรับข้อความไทยยาว ๆ (ชื่อวิชา/สาขา) และเคสข้อมูลขาด: "ไม่ทราบ", "จัดสอบเอง", เทอมที่ไม่มีข้อมูล, session หมดอายุ
- Stack ที่ codebase ใช้อยู่: Svelte 5 (runes) + WXT (บน Vite) + Tailwind v4, snapdom, date-fns, remeda

ยังไม่ตัดสิน / นอกขอบเขตรอบนี้:

- หน้าเกรด (`[ ]` ใน README) และหน้าอื่นของ reg — ไม่อยู่ในขอบเขต redesign รอบนี้
- **Dark mode** — ยังไม่ถูกกำหนดเป็นข้อบังคับของ product เลือกได้ตอนตัดสินใจดีไซน์

## Brand Commitments

- ชื่อ **KMITL +** และไอคอนชุดเดิม (`public/icon-*.png`) ตายตัว ห้ามเปลี่ยน identity
- **สีประจำสถาบัน สจล. เป็นสีของระบบ** (เจ้าของโปรเจกต์กำหนดจากคู่มือแบรนด์ของ สจล.):
  - Main: `#E35205` (Pantone 166c, DIC 1511, RGB 227 82 5)
  - Secondary: `#FFFFFF`, `#666666`, `#000000`
  - สีส้มถูกสงวนไว้ให้สถานะเท่านั้น (ข้อผิดพลาด, focus, การเลือกข้อความ)
    ไม่ใช้เป็นพื้นตกแต่ง เพราะจะแย่งความเด่นจากสีวิชา
  - เดิมรายการนี้มี "เวลาปัจจุบัน" ด้วย — เส้นเวลาปัจจุบันในตารางถูกถอดออกแล้ว
    ตอนนี้จึงไม่มีที่ไหนในกริดที่ใช้สีส้มเป็นพื้น ("วันนี้" กับ "สอบถัดไป" ไม่เคยถูกสร้าง)
  - **สีวิชาไม่นับรวมในชุดนี้** — เป็นของนักศึกษา แต่งเองได้ และห้ามชนกับสีส้มของระบบ
- ภาษาของ UI คือภาษาไทย
- License: ดู `LICENCE`

## Evidence on Hand

- Spec ของพฤติกรรมจริงที่ verify กับหน้าเว็บจริงแล้ว: `openspec/specs/{study-table-render,study-table-navigation,exam-schedule-render,image-export,theme-customize}/spec.md`
- Implementation ปัจจุบัน: `src/features/study-table/`, `src/features/exam-schedule/`, `src/core/boot.js`, `src/services/reg.js`, `src/shared/theme.js`
- **ไม่มี**: จำนวนผู้ใช้จริง, รีวิว, สถิติการใช้งาน, testimonial, ตัวเลข Chrome Web Store — ห้ามแต่งขึ้นมาอ้าง

## Product Principles

1. **หน้าเดิมต้องไม่หาย** — scrape พังเมื่อไหร่ ปล่อยหน้าเดิมของ reg ทำงานต่อ ไม่ใช่จอขาว และปุ่ม Old Design ต้องอยู่เสมอ
2. **อ่านได้ใน 2 วินาที** — งานหลักคือกวาดตาหาว่าวันนี้เรียนอะไร ลำดับสายตาต้องรับใช้การกวาด ไม่ใช่การอ่านทีละบรรทัด
3. **สิ่งที่เห็นบนจอ = สิ่งที่ได้ในรูป** — ดีไซน์ต้อง survive การ capture ของ snapdom; ตัวควบคุมไม่ติดไปในรูป
4. **ข้อมูลจริงเท่านั้น** — ทุกค่าที่แสดงมาจาก reg; ข้อมูลที่ขาดแสดงเป็น "ไม่ทราบ"/ว่าง ไม่เดาแทน
5. **ไทยเป็นพลเมืองชั้นหนึ่ง** — encoding, ฟอนต์, ความยาวข้อความ, ปี พ.ศ. ต้องถูกต้องก่อนความสวย

## Accessibility & Inclusion

- ข้อความไทยต้องอ่านออกที่ขนาดจริงของกริด (ชื่อวิชายาวและช่องแคบเป็นเคสปกติ)
- สีวิชาถูกผู้ใช้เปลี่ยนเองได้ — ห้ามให้ความหมายใด ๆ ขึ้นกับสีเพียงอย่างเดียว
- ยังไม่มีมาตรฐาน a11y ที่ผูกเป็นข้อบังคับของ product
