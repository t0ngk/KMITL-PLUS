# Tasks — refactor-study-grid-css-grid

## 1. Reference

- [x] 1.1 ~~On the live study table page with the *current* (pre-refactor) build, save screenshots plus a snapdom PNG export as fidelity references for every later task~~ — **superseded, the pre-refactor baseline is unrecoverable.** Instead: on the live study table page with the refactored build, walk and record the grid with a normal term, the empty-term state, the old-design toggle, and one snapdom PNG export. These become the reference for future changes to this page, and the acceptance evidence for tasks 4.1 and 4.2 here.
  - **Why superseded**: the `<table>` + colspan version of `Grid.svelte` no longer exists anywhere. Nothing between the `legacy` branch and the current tree was ever committed (`git log -1` → `932394e`, 2024-11-10), `git stash list` is empty, and `grep -rl colspan src/ preview/ .impeccable/` returns nothing. Section 3 landed against a state that cannot be reconstructed, so the pixel-for-pixel diff this task was written to enable is not producible by anyone.
  - **Consequence**: the "visually identical" claim in proposal.md is now supported by the structural argument recorded in tasks 3.1–3.3 (the border ledger, the grid-coordinate math, the `bg-white group-hover:` strip fix) plus a human acceptance walk — not by an image diff. A reader must not read `[x]` on 3.1/3.2 as "pixel-verified"; both say "structural verification only".
  - ~~Blocked on a logged-in session~~ — replaced by the method described below.
  - **Reference captured.** Taken from the built extension running in real Chrome on the genuine `report_studytable_show.php` URL with the redacted `study-table-mega` fixture served: the populated grid, the empty-term state, the old-design view, and a real snapdom PNG export. These become the reference for future changes to this page.
  - They are not a *baseline* in the original sense and never can be — the pre-refactor renderer no longer exists anywhere (see above). They are a forward reference only.
  - `.impeccable/review/{desktop,desktop-1280,menu}.png` were retaken from the preview harness against this revision. `now-line.png` was deleted when the current-time line was removed from the grid, which also means these captures show a grid with no time marker.

## 2. Scope table styling

- [x] 2.1 In `src/assets/styles.css`, move the `td`/`th`/`tbody` element rules under a `.kmitl-table` scope; add `class="kmitl-table"` to the `<table>` in `ExamSchedule.svelte` and to a wrapper around `{@html oldTable}` in `StudyTable.svelte`. Verify the exam schedule page and the old-design toggle look unchanged against the 1.1 references, and `grep -rn "<table\|<td\|<th" src/` shows no unscoped consumers.
  - Deviation from the literal wording, both caused by `refactor-exam-ui` landing first: (a) the exam `<table>` does **not** get `.kmitl-table` — `ExamTable.svelte` now re-declares those declarations as explicit classes, so the scope class would only duplicate them; (b) `{@html oldDesign}` in `ExamSchedule.svelte` **also** got a `.kmitl-table` wrapper. That legacy exam HTML consumed the same globals and `content/examSchedule.js` strips the registrar's own stylesheet, so scoping without it would have changed the exam old-design view — a consumer the task text did not list.
  - `grep -rn "<table\|<td\|<th" src/` → only `ExamTable.svelte`, which owns its styling per (a). `Grid.svelte` no longer contains any table element.
- [x] 2.2 Remove `* { transition-all }` from `styles.css`; add `transition-colors` to elements with hover/active color changes (day rows come in task 3; buttons in `Controls.svelte`, `CustomizeMenu.svelte`, `DownloadButton.svelte`). Verify by walking the UI (hover rows, open customize menu, switch term, toggle mode) — every color fade that existed before still fades.
  - `* { @apply font-prompt }` kept; `transition-all` removed. Every button/select in `Controls.svelte`, `CustomizeMenu.svelte` and both `DownloadButton` call sites already carries an explicit `transition-all` class, so removing the global rule cannot change them — no `transition-colors` was added there (it would be a redundant class). `grep -rn "hover:" src/ | grep -v transition` returns nothing after task 3.1. The day row is the only element that depended on the global rule.

## 3. Rewrite Grid.svelte as CSS Grid

- [x] 3.1 Replace the `<table>` markup with the CSS Grid structure from design.md: outer container (`max-content repeat(48, minmax(0,1fr))` columns, `auto repeat(7, minmax(0,1fr))` rows, `h-screen`, white bg, shadow, rounded-b), header row of 12 hour labels each spanning 4 tracks, and 7 subgrid day-row wrappers with `hover:bg-slate-100 transition-colors`, each containing its day label and 48 static bordered background cells. Verify gridlines, hour labels, and row hover match the 1.1 screenshot with no subjects considered yet.
  - Border layout needed care that design.md's "same CSS as today" glosses over: the old `<table>` renders with `border-collapse: collapse` (Tailwind preflight), so adjacent `border-l`/`border-r` merged into single 1px lines. CSS Grid has no collapsing, so borders are declared once per line instead: header cells get `border-t border-l` (last one also `border-r`), the body wrapper gets `border` (this is the old `tbody` border and the header/body divider), the day label gets none, and each of the 48 background cells gets `border-l` only. Same line positions, still 1px each.
  - Structural verification only (grid math, border ledger); the visual diff against 1.1 is still open.
- [x] 3.2 Replace `createTimeSlot` and the colspan-collapse pass with a direct mapping: each schedule item → `{ column-start, column-end, row }` from its `start`/`end` (15-minute slots, START_HOUR offset) and `dayIndex`; render subject cards into their day-row subgrid at `grid-row: 1` with z-index above the background cells, keeping the existing card internals (building/room line, centered name, section/type/time footer, `getTheme` background). Verify a real term renders every subject at the same position and size as the 1.1 screenshot, and that term switching still re-renders the grid.
  - `cardsByDay` stays a `$derived` over the `schedule` prop, so term switching re-renders as before.
  - One case design.md's decision 3 did not cover: the card wrapper keeps the old `<td>`'s `py-1`, so the background cells' 15-minute gridlines would have shown through the 4px strips above and below each card — lines the old colspan `<td>` did not draw. The card wrapper therefore carries `bg-white group-hover:bg-slate-100 transition-colors` (matching the row's rest and hover states) plus its own `border-l` for the card's left edge; its right edge comes from the next background cell's `border-l`, exactly as the old collapsed borders did.
  - Structural verification only; the screenshot diff is still open.
- [x] 3.3 Confirm edge behavior matches the old renderer: empty-term state unchanged, a schedule item at 08:00 and one ending 20:00 sit flush with the grid edges, and two items overlapping in time render with the later source item painted on top (current last-writer-wins outcome).
  - ~~Blocked on live verification~~ — **unblocked and verified** by `preview-harness-fixtures`. The `study-table-mega` fixture is a real registrar page (redacted) with rows spliced in specifically to produce these three cases, and the preview drives the real component through the real scraper.
  - *Empty term* — reached by selecting semester 2 in the preview; `StudyTable.svelte` renders `ไม่มีข้อมูลภาคเรียนนี้` and `Grid.svelte` is never mounted, exactly as the construction argument claimed.
  - *08:00 and 20:00 edges* — the `EARLY MORNING EDGE CASE` block (08:00–09:00) sits flush against the left border and `LATE EVENING EDGE CASE` (19:00–20:00) flush against the right, confirmed in the rendered page and in the snapdom export.
  - *Overlap* — `OVERLAP LOWER LAYER` (Fri 14:00–16:00) and `OVERLAP UPPER LAYER` (Fri 15:00–17:00) render with the later source item painted over the earlier one. Last-writer-wins is now observed rather than argued.
  - Nothing about this needed a live session: the fixture supplies the data the account never had (the whole real corpus starts no earlier than 09:00 and ends no later than 19:30, with zero overlaps).

## 4. Verify integration

- [x] 4.1 Export a snapdom PNG from the refactored grid and check it against the `study-table-render` spec and the shipped border ledger: gridlines land on the hour-label boundaries, row separators and the body-wrapper border are single 1px lines, subject cards sit flush at 08:00 and 20:00, and fonts and theme colors render as on screen.
  - Rewritten twice. First from "compare side-by-side with the 1.1 export" (see 1.1: no pre-refactor export exists). Then again here: the criterion said *15-minute* gridlines, which the shipped grid does not draw. `Grid.svelte` renders one bordered background cell per hour and states the rule in a comment (`เส้นที่วาดจริงมีแค่ขอบชั่วโมง`); `DESIGN.md` records it as intent. **design.md decision 3 is stale on this point and has been annotated** — checking against it as written would have failed a grid that is behaving as designed.
  - Verified against the shipped design on a real snapdom export (`2784x1592` at `scale: 2`, from `preview-harness-fixtures`): 12 single-1px vertical lines at the hour boundaries and nothing between them, single 1px row separators, cards flush at both time edges, Thai text in the embedded Prompt font, theme tints matching the screen. No floating controls in the image.
  - ~~Blocked~~ — no live session needed; the export comes from a real registrar page rendered through the real component.
- [x] 4.2 Run `pnpm lint` and `pnpm build`, load the built extension in Chrome, and walk the study table page end-to-end (load, hover, customize theme, switch term, toggle old design, export) with no console errors.
  - `pnpm lint` clean and `pnpm build` succeeds (now a WXT build — see `migrate-to-wxt`).
  - **The Chrome walk is done.** The built extension was loaded unpacked in real Chrome and driven on the genuine `report_studytable_show.php` URL, with registrar requests intercepted and answered from the committed redacted fixtures. Every step passed with **zero console errors**: the grid rendered, the customize menu opened, term switching went through `services/reg.js` (semester 2 → empty state → back to the same placements), the old-design toggle showed the real registrar table and returned, and PNG export downloaded 386 kB with Thai text in Prompt.
  - The old-design step is the one that matters most for this change: it is the first time the `.kmitl-table` scoping from task 2.1 has been observed styling genuine registrar markup **inside the shipped extension**, rather than in the preview harness.
  - Remaining gap, stated rather than glossed: the registrar's own stylesheet was answered `204`, so this does not show reg's CSS interacting with the scoped selectors. That is the same gap `preview-harness-fixtures` design.md records as untestable without the live site.
